import React from 'react';
import Loc from './Loc';

function Vipin(props) {
    return (
        <div>
            Vipin is learning {props.course} progress
        </div>
    );
}

export default Loc(Vipin);
